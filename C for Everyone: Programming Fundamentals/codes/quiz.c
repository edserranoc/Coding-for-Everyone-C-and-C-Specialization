/*
Description:
This program calculates the sine and cosine of angles from 0 to 1.
It prints a table with the angle in radians and its corresponding 
sine value.
*/

#include<stdio.h>
#include<math.h>

int main(void){ 
    
    int *q;
    int y;
    y = *q;
    printf("%d\n", *q);
    return 0;
}